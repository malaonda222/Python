# No Users

user_names = ["admin", "Sarah", "Victoria", "Jaden", "Eric"]

empty_user_names = user_names.clear()

if user_names == []:
    print("The list is empty, we need to find some users!")

if not user_names:
    print("The list is empty, we need to find some users!")
